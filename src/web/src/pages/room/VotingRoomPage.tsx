import { useNavigate } from 'react-router-dom';

export function VotingRoomPage(props: { roomId: string; voterId: string }) {
  const navigate = useNavigate();

  return <h1>TODO: Voting here</h1>;
}
